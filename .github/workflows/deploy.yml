name: Deploy to Cloudflare Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
name: Deploy to Cloudflare Pages

on:
  push:
      branches: [ "main" ]
        workflow_dispatch:
        
        jobs:
          deploy:
              runs-on: ubuntu-latest
                  steps:
                        - uses: actions/checkout@v4
                        
                              - name: Install pnpm
                                      uses: pnpm/action-setup@v4
                                              with:
                                                        version: 10
                                                        
                                                              - name: Use Node.js 20.x
                                                                      uses: actions/setup-node@v4
                                                                              with:
                                                                                        node-version: 20.x
                                                                                                  cache: 'pnpm'
                                                                                                  
                                                                                                        - name: Install dependencies
                                                                                                                run: pnpm install
                                                                                                                
                                                                                                                      - name: Build
                                                                                                                              run: pnpm build
                                                                                                                              
                                                                                                                                    - name: Deploy to Cloudflare Pages
                                                                                                                                            uses: cloudflare/wrangler-action@v3
                                                                                                                                                    with:
                                                                                                                                                              apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
                                                                                                                                                                        accountId: dec9a6cbc391c2584adcc6e7cb98fb85
                                                                                                                                                                                  command: pages deploy dist/public --project-name=a1tradelines --commit-dirty=true
                                                                                                                                                                                          env:
                                                                                                                                                                                                    HUBSPOT_API_TOKEN: ${{ secrets.HUBSPOT_API_TOKEN }}deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: dec9a6cbc391c2584adcc6e7cb98fb85
          command: pages deploy dist/public --project-name=a1tradelines2 --commit-dirty=true
        env:
          HUBSPOT_API_TOKEN: ${{ secrets.HUBSPOT_API_TOKEN }}
