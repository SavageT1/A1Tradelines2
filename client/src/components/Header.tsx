import { useEffect, useState } from "react";
import { AnimatePresence, motion } from "framer-motion";
import { Link, useLocation } from "wouter";
import { Menu, X } from "lucide-react";
const links = [{ href: "/buy-tradelines", label: "Browse Tradelines" }, { href: "/tradeline-assessment", label: "Get Matched" }, { href: "/tradeline-glossary", label: "Credit Terms" }, { href: "/broker-program", label: "Broker Program" }, { href: "/blog", label: "Learning Center" }];
export default function Header() { const [location] = useLocation(); const [open, setOpen] = useState(false); const [scrolled, setScrolled] = useState(false); useEffect(() => { const f = () => setScrolled(window.scrollY > 12); window.addEventListener("scroll", f, { passive: true }); return () => window.removeEventListener("scroll", f); }, []); useEffect(() => setOpen(false), [location]); return <header className={`fixed inset-x-0 top-0 z-50 border-b border-lime-400/40 bg-gradient-to-r from-[#050914] via-[#13284d] to-[#03060d] text-white shadow-[0_4px_24px_rgba(113,216,53,.2)] transition-all duration-300 ${scrolled ? "shadow-[0_5px_30px_rgba(113,216,53,.28)]" : ""}`}><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 sm:px-8 lg:px-12"><Link href="/" className="flex items-center gap-3" aria-label="A1 Tradelines home"><span className="flex h-12 w-12 items-center justify-center rounded-sm bg-black/80 ring-1 ring-white/20"><img src="/logo.png" alt="A1 Tradelines" className="h-11 w-auto object-contain"/></span><span className="hidden text-sm font-black tracking-[.16em] text-white sm:inline">TRADELINES</span></Link><nav className="hidden items-center gap-1 xl:flex">{links.map(link => <Link key={link.href} href={link.href} className={`rounded px-3 py-2 text-xs font-black uppercase tracking-[.1em] transition-colors ${location === link.href ? "text-lime-300" : "text-white/80 hover:text-lime-300"}`}>{link.label}</Link>)}</nav><Link href="/contact" className="hidden bg-[#12213f] px-5 py-3 text-xs font-black uppercase tracking-[.12em] text-white ring-1 ring-lime-300/30 transition hover:bg-blue-700 xl:inline-flex">Private Consultation <span className="ml-3">→</span></Link><button type="button" onClick={() => setOpen(!open)} className="rounded p-3 text-white xl:hidden" aria-expanded={open} aria-label="Toggle menu">{open ? <X/> : <Menu/>}</button></div><AnimatePresence>{open && <motion.nav initial={{ opacity: 0, y: -10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -10 }} className="border-t border-lime-400/30 bg-[#071124] px-5 py-4 xl:hidden">{links.map(link => <Link key={link.href} href={link.href} className="block py-3 text-sm font-black uppercase tracking-[.1em] text-white/85">{link.label}</Link>)}<Link href="/contact" className="mt-2 block bg-[#12213f] px-4 py-3 text-center text-sm font-black uppercase tracking-[.1em] text-white ring-1 ring-lime-300/30">Private Consultation</Link></motion.nav>}</AnimatePresence></header>; }
