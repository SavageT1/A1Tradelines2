import { useEffect, useState } from "react";
import { AnimatePresence, motion } from "framer-motion";
import { Link, useLocation } from "wouter";
import { Menu, X } from "lucide-react";
const links = [{ href: "/buy-tradelines", label: "Browse Tradelines" }, { href: "/tradeline-assessment", label: "Get Matched" }, { href: "/tradeline-glossary", label: "Credit Terms" }, { href: "/broker-program", label: "Broker Program" }, { href: "/blog", label: "Learning Center" }];
export default function Header() { const [location] = useLocation(); const [open, setOpen] = useState(false); const [scrolled, setScrolled] = useState(false); useEffect(() => { const f = () => setScrolled(window.scrollY > 12); window.addEventListener("scroll", f, { passive: true }); return () => window.removeEventListener("scroll", f); }, []); useEffect(() => setOpen(false), [location]); return <header className={`fixed inset-x-0 top-0 z-50 border-b transition-all duration-300 ${scrolled ? "border-slate-200 bg-[#f4f5f7]/90 shadow-sm backdrop-blur-xl" : "border-slate-200/70 bg-[#f4f5f7]/75 backdrop-blur-md"}`}><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 sm:px-8 lg:px-12"><Link href="/" className="flex items-center gap-2" aria-label="A1 Tradelines home"><img src="/logo.png" alt="A1 Tradelines" className="h-12 w-auto object-contain"/><span className="hidden text-sm font-black tracking-[.16em] text-[#12213f] sm:inline">TRADELINES</span></Link><nav className="hidden items-center gap-1 xl:flex">{links.map(link => <Link key={link.href} href={link.href} className={`rounded px-3 py-2 text-xs font-black uppercase tracking-[.1em] transition-colors ${location === link.href ? "text-blue-700" : "text-[#12213f] hover:text-blue-700"}`}>{link.label}</Link>)}</nav><Link href="/contact" className="hidden bg-[#12213f] px-5 py-3 text-xs font-black uppercase tracking-[.12em] text-white hover:bg-blue-700 xl:inline-flex">Private Consultation <span className="ml-3">→</span></Link><button type="button" onClick={() => setOpen(!open)} className="rounded p-3 text-[#12213f] xl:hidden" aria-expanded={open} aria-label="Toggle menu">{open ? <X/> : <Menu/>}</button></div><AnimatePresence>{open && <motion.nav initial={{ opacity: 0, y: -10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -10 }} className="border-t border-slate-200 bg-[#f4f5f7] px-5 py-4 xl:hidden">{links.map(link => <Link key={link.href} href={link.href} className="block py-3 text-sm font-black uppercase tracking-[.1em] text-[#12213f]">{link.label}</Link>)}<Link href="/contact" className="mt-2 block bg-[#12213f] px-4 py-3 text-center text-sm font-black uppercase tracking-[.1em] text-white">Private Consultation</Link></motion.nav>}</AnimatePresence></header>; }
